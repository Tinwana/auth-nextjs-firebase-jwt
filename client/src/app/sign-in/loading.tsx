const Loading = () => {
  return (
    <div className="bg-[rgba(0,0,0,.7)] flex items-center justify-center h-screen w-screen z-50">
      <p className="text-5xl">Loading...</p>
    </div>
  );
};

export default Loading;
